import os
import sys
import transaction

from sqlalchemy import engine_from_config

from pyramid.paster import (
    get_appsettings,
    setup_logging,
    )

from ..models import (
    DBSession,
    Base,
    )

def main(argv=sys.argv):
    os.system('createdb -U postgres {{package}}')
    os.system('alembic upgrade head')
